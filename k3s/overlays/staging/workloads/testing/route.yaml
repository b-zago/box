apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: testing
  labels:
    app: testing
spec:
  parentRefs:
    - name: staging-gateway
      kind: Gateway
  hostnames:
    - testing.dev.zagoapps.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: testing
          port: 80
